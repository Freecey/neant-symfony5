{% extends 'base.html.twig' %}

{#{% block title %}#}

{% block body %}

    <div class="container mx-auto">
        <h2>Edit Article</h2>
        {{ form_start(form) }}
            {{ form_widget(form) }}
        <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full">Save change</button>
        {{ form_end(form) }}
    </div>

{% endblock %}
